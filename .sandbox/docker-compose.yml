services:
  opencode:
    build:
      context: .
      dockerfile: Dockerfile.opencode
    command: sleep infinity
    volumes:
      - ../:/workspace
      - ./opencode:/home/ubuntu/.config/opencode