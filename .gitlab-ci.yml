stages:          # List of stages for jobs, and their order of execution
  - test

test_template:   # This job runs in the test stage.
  stage: test    # It only starts when the job in the build stage completes successfully.
  tags:
    - docker
  image: python:3.13
  before_script:
      - apt update
      - apt install -y chromium
      - pip install -e .
      - pip install -r ./tests/requirements.txt
  script:
      - nohup pyplet start > pyplet.log 2>&1 &
      - ps -aux | grep pypley
      - python3.13 ./tests/test_template.py
      - pkill -f "pyplet start"
